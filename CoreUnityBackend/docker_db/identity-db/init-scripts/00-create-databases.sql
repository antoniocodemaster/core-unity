-- Eliminar las bases si existen (evitar errores)
DROP DATABASE IF EXISTS core_unity_identity;
DROP DATABASE IF EXISTS core_unity;

-- Crear nuevas bases
CREATE DATABASE core_unity_identity;
CREATE DATABASE core_unity;